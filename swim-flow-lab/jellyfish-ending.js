(()=>{function boot(){const section=document.getElementById('water-ending');if(!section)return;const video=section.querySelector('video'),button=section.querySelector('button'),reduced=matchMedia('(prefers-reduced-motion: reduce)');let visible=false,userPaused=false;video.muted=true;function sync(){button.textContent=video.paused?'再生':'一時停止';button.setAttribute('aria-label',video.paused?'クラゲの動画を再生':'クラゲの動画を一時停止')}function update(){if(visible&&!document.hidden&&!userPaused&&!reduced.matches)video.play().catch(sync);else video.pause()}button.addEventListener('click',()=>{if(video.paused){userPaused=false;video.play().catch(sync)}else{userPaused=true;video.pause()}});video.addEventListener('play',sync);video.addEventListener('pause',sync);new IntersectionObserver(entries=>{visible=entries[0].isIntersecting;document.body.classList.toggle('at-water-ending',visible);update()},{threshold:0.15}).observe(section);document.addEventListener('visibilitychange',update);reduced.addEventListener('change',update);sync()}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot()})();

